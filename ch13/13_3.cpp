#include <iostream>
/*
final,etc:

final: we use this term for control. Final is used for variables, classes or methods that are changable.

finally: this term is used for try/catch methods. its used so a segment of code will always be executed

finalize: written as finalize(); is actually a method that we use when the garbage collector sees that all refrences have been used 
*/
int main() {
  std::cout << "Hello World!\n";
}
