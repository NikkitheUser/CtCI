#include <iostream>
/*
Sort Big file:

Lets try a externalsort method, starting with dividing the file into x megabytes. 
x memory is what we have available.
each divided file is then sorted and saved back into our file system.
once its all sorted, we can merge everything back together, now we have a fully sorted file. 
*/
int main() {
  std::cout << "the answer is in the commment";
}
